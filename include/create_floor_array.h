/**
 * @file create_floor_array.h
 * @briefs declaration for create_floor_array function -creates an array for floors with respective to input entered by user.
   Returns the array of floors. 
 */
 #ifndef __CREATE_FLOOR_ARRAY__
#define __CREATE_FLOOR_ARRAY__

extern struct passenger * create_floor_array(int no_of_floors);

#endif
